## Required modules:

- **Beneficiary**
  - Add (Call sms service when beneficiary is added)
- **Vendor**
  - Add
  - SendOtp (Call sms service, save otp hash in database)
  - VerifyOtp (Call transfer token service)

## Call external services:

- SMS service
- Token transfer service
